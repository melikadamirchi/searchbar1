import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './tamrin20mordad/App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render( <
    React.StrictMode >
    <
    App / >
    <
    /React.StrictMode>
);